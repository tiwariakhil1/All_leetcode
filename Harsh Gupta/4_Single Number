# Method 1: Us`1qes xor Concept {1. 

class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int s=nums.size(),single=0;
        for(int i=0;i<s;i++)
        {
            single^=nums[i];
        }
        return single;
    }
};
